// 模板项目
# 模板项目架构设计

## 整体架构

采用 Nginx 作为统一网关，下方部署多个子服务（用户服务、订单服务、商品服务等），每个子服务内部采用 API + RPC 分层架构。

外部请求流程：客户端 → Nginx 网关 → 对应子服务的 API 层 → RPC 层 → 数据库/缓存

服务间调用流程：子服务 A 的 RPC 层 → gRPC 直接调用 → 子服务 B 的 RPC 层



## 核心设计

**1. Nginx 统一网关**
所有外部请求通过 Nginx 分发到对应的子服务 API 层，Nginx 负责负载均衡、SSL 终止、限流等通用功能。

**2. 每个子服务采用 API + RPC 分层**
- API 层：处理 HTTP 请求、参数校验、JWT 鉴权，对外暴露 RESTful 接口
- RPC 层：承载核心业务逻辑、数据库操作、事务处理，通过 gRPC 提供高性能内部接口

**3. 服务间通过 gRPC 直接通信**
子服务之间调用不走 HTTP，直接通过 gRPC 调用对方的 RPC 服务。gRPC 基于 Protobuf 序列化，性能比 HTTP/JSON 高 5-10 倍，延迟更低，适合高频内部调用。

## 典型调用示例

用户创建订单场景：
客户端发起 HTTP 请求到 /user/create-order → Nginx 转发到用户服务的 API 层 → API 调用本地 RPC 层 → 用户服务 RPC 通过 gRPC 调用订单服务 RPC 创建订单 → 订单服务 RPC 调用商品服务 RPC 扣减库存 → 逐层返回结果 → 最终响应客户端

整个过程中，外部请求走 HTTP，内部服务间调用全部走 gRPC，保证高效通信。



## 扩展优势

**新增服务简单**：只需在 Nginx 添加路由配置，按照 API+RPC 模式开发新服务即可接入系统

**服务间高效通信**：内部调用走 gRPC，避免 HTTP 的序列化和协议开销，显著降低延迟

**独立部署和扩缩容**：每个子服务可单独构建镜像、独立部署、根据负载独立扩容，互不影响

**技术栈统一**：所有子服务遵循相同的 API+RPC 架构模式，降低团队学习成本和维护复杂度

**平滑演进**：初期可以只部署单个子服务，随着业务增长逐步拆分出更多子服务，架构无需大改


## 技术栈

Go 1.23.7 + go-zero v1.7.3 | gRPC + Protocol Buffers | Nginx 网关 | MySQL + Redis

# 在rpc 模块中
go get gorm.io/gorm@v1.25.7
go get gorm.io/driver/mysql@v1.5.7  # MySQL
